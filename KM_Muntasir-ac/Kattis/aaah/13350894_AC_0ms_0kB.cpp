#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s,p;
    getline(cin,s);
    getline(cin,p);
    if(s.length() >= p.length())
        cout<<"go"<<endl;
    else
        cout<<"no"<<endl;
    return 0;
}


